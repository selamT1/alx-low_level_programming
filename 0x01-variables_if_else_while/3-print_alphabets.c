#include <stdio.h>
#include <stdlib.h>
/**
 * main -while loop
 * description: print capital and small letters
 * Return: 0
 */
int main(void)
{
	char lc = 'a';
	char UC = 'A';

	while (ls <= 'z')
	{
		putchar(ls);
		ls++;
	}
	while (UC <= 'Z')
	{
		putchar(UC);
		UC++;
	}
	putchar('\n');
}
