#include "main.h"
/**
 * add - add two integeers:
 * @a:first arg accept int values
 * @b: second arg
 * Return: Always sum of the two integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
